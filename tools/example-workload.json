{
  "queries": [
    {
      "label": "homepage_sortAt",
      "filters": [
        {"Eq": ["nsfwLevel", {"Integer": 1}]},
        {"Not": {"Eq": ["availability", {"String": "Private"}]}}
      ],
      "sort": {"field": "sortAt", "direction": "Desc"},
      "limit": 20
    },
    {
      "label": "user_lookup",
      "filters": [
        {"Eq": ["userId", {"Integer": 1}]}
      ],
      "sort": {"field": "sortAt", "direction": "Desc"},
      "limit": 20
    },
    {
      "label": "filter_only",
      "filters": [
        {"Eq": ["nsfwLevel", {"Integer": 1}]}
      ]
    }
  ]
}
