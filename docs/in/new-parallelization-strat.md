Okay, so I'm thinking about how we handle fast throughput when we're doing bulk loading or whatever. And we took an approach with the parallel flush pipeline branch that works, but I think it's actually probably overly complicated, and I think we can simplify things. There are things that we should take and things that we can leave. The things that we should take include both of the worker pools, the pool for decomposing the field, and the pool for the field. and accumulating them to essentially buffers to be applied, and then promoting them to queues, slots, for the second pool of workers to then be applying those to bitmaps. And previously that apply process was, you know, apply to a diff, and then eventually that diff gets applied to a staging version of the target bitmap, and then they get swapped to keep readers from having to deal with contention there. of having to have locks wait for locks But I think what I want to do instead is mostly keep that but actually simplify things Instead of having everything go through the put channel and having a back pressure and all that stuff instead we have two endpoints put and put And put will work basically like the way that we have it right now but no back pressure thing So it go and get decomposed by the decomposition pool and the decomposition pool based on if it coming through put or put will apply a sort of priority that we have to do with the buffer. And that buffer should work for both things coming through bulk and the standard channel. But what priority does, if it's, you know, bulk priority versus standard priority, if it's bulk priority, it won't ever be automatically promoted to the slots to be picked up by the second pool of workers. Instead, it'll, you know, eventually be picked up once its queue is big enough that it deserves to be promoted. But in standard priority mode, we essentially have the that we have a contract to promote that thing to the slot once we hit a certain amount of time So we have to come up with some mechanism for that where I guess we checking in some sort of background thread periodically if we need to force something into a slot And that probably needs to flag that queue that we just promoted to that slot so that when the accumulators continue to try and load stuff in they won replace that slot because it needs to be picked up by a worker to kind of meet that guarantee So that kind of the difference between standard and bulk And then downstream in the worker pool, another difference is if it's a standard apply, then we need to do the diff thing, where we apply to a diff, and then eventually we apply that diff to a stage copy or whatever, and then we swap those. So that kind of how we would handle standard puts But for bulk puts instead we essentially recognizing that we loading things as quickly as we can and we comfortable blocking the read path for just a second So rather than applying to diff and then the diff eventually being applied to the target after a swap and all of that dance We would instead just apply directly to main as the apply workers are draining their slot So should reduce the number of operations that the apply workers are doing dramatically when they processing a bulk queue item versus a standard queue item I not completely sure if we want to keep standard versus bulk that are kind of separate, and essentially not have everything in the bulk queue get promoted to standard when that happens. Mostly because, you know, the dance there is so slow. But I guess it doesn't really matter because if it's applying, it's applying. So you might as well just have it be, you know, that dance, even if it is, you know, a bunch of things being promoted to standard. Because essentially the standard's us saying, hey, we want to have a guarantee that we're not going to block the read path when we go through standard. But if you're doing in bulk, then it's okay. Let me know if you have any questions or thoughts about this, or if it's going to be problematic based on anything else that we saw while we were doing the parallel flush pipeline stuff.

something that I feel like we haven't really properly addressed is the dock, the persisting. I guess in the parallel flush, we had fixed thresholds where we now then would flush from memory to disk as well. And I guess that's just to make sure that the disk isn't getting too far behind. I guess for standard versus bulk, I don't know that we need to do anything different there. I kind of imagine that the doc saving side of things could be yet another kind of pool of workers that are essentially batching saves to readdb. We don't want to be building up a massive library of documents in memory that's going to be expensive. So probably what needs to happen is accumulators are also putting things into a queue queue for the doc batchers where they going to save those docs to disk and then the apply workers are also putting something into another queue for doc saving So essentially there a doc pool and probably could be a similar queuing mechanism where you basically throwing in actual documents to be saved and then there just workers churning through those probably applying in batches It could probably be a fixed size I guess it would be cool if they scaled with back pressure so like did bigger batches if they had more to do. I don't know if that's a balancing act, because sometimes bigger batches take longer to save, so they get further and further behind. I don't know, we'll have to figure out that side. And then the apply workers would essentially be saying, hey, this bitmap needs to be saved. So they'd be putting something into a queue saying, hey, this bitmap over here needs to be saved, and then it can apply those in bulk too. And then there would essentially just be a queue of of doc saving workers or whatever. I don't know that we necessarily need a slot mechanism there. I think that they can just be saving things in bulk batches. I guess we would need to figure out the right way to do coordination there. I don't know. I'm not sure about this one. You'll have to let me know.